{
  "$schema": "https://www.schemastore.org/package.json",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && node scripts/prepare-assets.cjs",
    "dev": "vite"
  },
  "dependencies": {
    "concurrently": "^9.0.1",
    "laravel-vite-plugin": "^3.1",
    "tailwindcss": "^4.0.0",
    "vite": "^8.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0"
  }
}
