{
  "version": 2,

  "buildCommand": "composer install && npm install && npm run build",

  "functions": {
    "api/index.php": {
      "runtime": "vercel-php@0.9.0"
    }
  },

  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.php"
    }
  ]
}